name: Actions

on: 
  push:
  pull_request:
    types: [closed,assigned,opened,reopened]  # added activity types

jobs:
  run-github-actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./simple.yml

      
